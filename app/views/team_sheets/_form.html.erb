<%= form_for(@team_sheet) do |f| %>
  <% if @team_sheet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_sheet.errors.count, "error") %> prohibited this team_sheet from being saved:</h2>

      <ul>
      <% @team_sheet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
