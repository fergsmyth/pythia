<%= form_for(@scrapper_job) do |f| %>
  <% if @scrapper_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scrapper_job.errors.count, "error") %> prohibited this scrapper_job from being saved:</h2>

      <ul>
      <% @scrapper_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
